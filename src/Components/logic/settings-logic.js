import settingsIcon from "../icons/setting.png";

const Settings = () => {
  return (
    <button>
      <img src={settingsIcon} id="settings_button" alt="ustawienia konta"></img>
    </button>
  );
};

export default Settings